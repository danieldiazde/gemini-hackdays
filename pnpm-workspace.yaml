packages:
  - 'apps/frontend'

# Native postinstall scripts we explicitly allow to run.
# `sharp` powers Next.js image optimization; `unrs-resolver` is the
# Rust-based module resolver used by eslint-config-next.
allowBuilds:
  msw: false
  sharp: true
  unrs-resolver: true
